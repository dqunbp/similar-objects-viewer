{
  "name": "similar-objects-viewer",
  "version": "1.0.0",
  "description": "gis objects view tool",
  "repository": {
    "type": "git",
    "url": "https://github.com/dqunbp/similar-objects-viewer.git"
  },
  "main": "index.js",
  "scripts": {
    "dev": "parcel index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "rm -rf dist && parcel build index.html --public-url /similar-objects-viewer",
    "deploy": "gh-pages -d dist"
  },
  "author": "dqunbp",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "d3-fetch": "^1.1.2",
    "parcel-bundler": "^1.10.3",
    "sass": "^1.14.3"
  }
}
